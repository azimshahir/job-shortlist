__pycache__/
*.pyc
scraper/raw_jobs.csv
scraper/filtered_jobs.csv
scraper/ranked_jobs.csv
scraper/final_jobs.csv
# shortlist.md is committed by the routine as the daily record
scraper/resumes/
# web
web/node_modules/
web/.next/
web/.env*.local
.env
.venv/
.env
# History now lives in Supabase (HISTORY_BACKEND=supabase). The local SQLite
# file is only for offline runs and is never committed.
scraper/job_history.db
scraper/job_history.db-journal
scraper/job_history.db-wal
# .env files are never committed (secrets live in Vercel + GitHub Secrets)
.env.*
